// routes
import { PATH_DASHBOARD } from '../../routes/paths';
const navConfig = [
  // GENERAL
  // ----------------------------------------------------------------------
  {
    subheader: 'Dahsboard',
    items: [
      { title: 'Home', path: PATH_DASHBOARD.home },

    ],
  },
];

export default navConfig;
